import * as React from "react";
import { motion } from "framer-motion";

const Ex = () => {
  return (
    <motion.a
        whileHover={{ scale: 1.2 }}
        onHoverStart={e => {}}
        onHoverEnd={e => {}}
    />
  )
}

export default Ex